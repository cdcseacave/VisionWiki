# Large binary files — PDFs are cached locally, re-downloadable via url: in wiki frontmatter
# Leading slash anchors the pattern to the repo root; otherwise it would also match wiki/papers/ and wiki/assets/
/papers/
/assets/

# Obsidian workspace files
.obsidian/

# OS files
.DS_Store
